# Функция для создания матрицы смежности из заданного графа
def create_adjacency_matrix(graph):
	# Создаемматрицуснуля,заполненнуюнулями
	matrix=[[0]*len(graph)for_inrange(len(graph))]

	# Заполняем матрицу смежности
	for i in range(len(graph)): # Цикл по всем строкам графа
		for j in range(len(graph[i])): # Цикл по всем столбцам текущей строки
			if graph[i][j]: # Проверяем, есть ли ребро между вершинами i и j
			matrix[i][j]=1 # Если есть ребро, устанавливаем элемент в 1

	# Возвращаем созданную матрицу смежности
	return matrix

# Пример использования функции
graph=[
	[0,1,1,0], # Вершины:0->1,0->2,1->2,2->3
	[1,0,1,1], # Вершины:1->0,1->2,2->3,3->2
	[1,1,0,1], # Вершины:2->0,2->1,3->1,3->2
	[0,1,1,0] # Вершины:3->1,3->2
]

adjacency_matrix=create_adjacency_matrix(graph)
print("Граф:")
for row in graph:
	print(row) # Выводим исходный граф для сравнения

print("\nМатрица смежности:")
for row in adjacency_matrix:
	print(row) # Выводим созданную матрицу смежности